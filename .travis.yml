language: python
cache: pip
sudo: false

python:
  - 3.5
  - 3.6

install:
  - pip install -r requirements.txt
  - pip install -r requirements-dev.txt

script:
  - ./ci/lint.sh
  - python setup.py test
