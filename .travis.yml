language: python
cache: pip
sudo: false

python:
  - 3.5
  - 3.6

install:
  - pip install -r requirements.txt
  - pip install -r requirements-dev.txt

script:
  - ./lint.sh
  - python setup.py test

addons:
  apt:
    packages:
      - pylint

notifications:
  email:
    on_success: never
